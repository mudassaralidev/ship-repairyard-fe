import { Outlet } from 'react-router-dom';

// ==============================|| LAYOUT - BLANK PAGES  used||============================== //

const PagesLayout = () => {
  return <Outlet />;
};

export default PagesLayout;
